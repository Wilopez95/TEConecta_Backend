import { TeconectaApplication } from './application';
import { ApplicationConfig } from '@loopback/core';
export { TeconectaApplication };
export declare function main(options?: ApplicationConfig): Promise<TeconectaApplication>;
